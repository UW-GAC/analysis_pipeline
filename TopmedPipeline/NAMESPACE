import(utils)
import(stats)
import(methods)
import(Biobase)
import(SeqArray)
importFrom(SeqVarTools, refChar, altChar)
importFrom(S4Vectors, subjectHits, queryHits)
importFrom(IRanges, IRanges)
importFrom(GenomicRanges, GRanges, findOverlaps)
importFrom(dplyr, arrange_, distinct_, filter_, mutate_, rename_, select_, inner_join, left_join, one_of, "%>%")
importFrom(tidyr, gather_, separate_)

export(getobj,
       readConfig,
       setConfigDefaults,
       writeConfig,
       addInvNorm,
       aggregateListByAllele,
       aggregateListByPosition,
       calculateLambda,
       countThreads,
       filterByChrom,
       filterByMAF,
       filterByPass,
       formatAssocSingle,
       getAssoc,
       getPhenotypes,
       insertChromString,
       intToChr,
       rankNorm,
       sequentialVariantIds)
