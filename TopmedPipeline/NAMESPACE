import(utils)
import(stats)
import(methods)
import(Biobase)
import(SeqArray)
importFrom(BiocGenerics, start, end)
importFrom(SeqVarTools, refChar, altChar, isSNV)
importFrom(S4Vectors, subjectHits, queryHits)
importFrom(IRanges, IRanges)
importFrom(GenomicRanges, GRanges, findOverlaps, resize, width, seqnames)
importFrom(dplyr, arrange_, distinct_, filter_, group_by_, mutate_, rename_,  select_, inner_join, left_join, n, one_of, sample_frac, row_number, ungroup, "%>%")
importFrom(tidyr, gather_, separate_)
importFrom(lazyeval, interp)

export(getobj,
       readConfig,
       setConfigDefaults,
       writeConfig,
       addInvNorm,
       aggregateListByAllele,
       aggregateListByPosition,
       calculateLambda,
       checkSelectedVariants,
       combineAssoc,
       constructFilename,
       countThreads,
       defineSegments,
       filterByChrom,
       filterByFile,
       filterByMAF,
       filterByPass,
       filterByPCAcorr,
       filterBySegment,
       filterBySNV,
       formatAssocSingle,
       getAssoc,
       getGRM,
       getPhenotypes,
       insertChromString,
       intToChr,
       omitKnownHits,
       rankNorm,
       subsetBySegment,
       sequentialVariantIds,
       thinPoints,
       writeSegmentFile)
